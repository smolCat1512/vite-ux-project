const Connect = () => {
  return (
    <div className="h-screen flex flex-col items-center justify-center p-4 w-11/12">
      <h2 className="text-3xl font-bold pb-6">Connect</h2>
      {/* Add form, links, buttons here */}
    </div>
  );
};

export default Connect;